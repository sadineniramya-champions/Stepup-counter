import React from “react”;
import ReactDOM from “react-dom/client”;
import StepUpCounter from “./StepUpCounter”;

ReactDOM.createRoot(document.getElementById(“root”)).render(
<React.StrictMode>
<StepUpCounter />
</React.StrictMode>
);